# where markdown files and templates are stored
#ROOT_FOLDER: /home/ok/strony/staticpie/pie/blog # This is going to be removed from config and I will assume that the config is itself inside the root folder
# where website is generated, preferable /tmp and absolute path
PUBLIC_FOLDER: /home/ok/strony/staticpie/pie/blog_public
# the URL used for deploy
BASE_URL: superblog.com
# the protocol http:// or https://
PROTOCOL: http://

# Folders that should be copied to public folder
includes:
    - assets
    - styles.css
    - styles.rtl.css

# Extensions
extensions:
    - menu
    - tags
    - pagelist
    - search
    - mostrecent
    
tags:
    tags_ignored:
        - movie
    tags_map:
        art: 
            label: Article
            order: 100
        art2:
            label: Annother article category
            order: 101